{
    "IsPicture": {
        "description": "查询作品时候是否发送封面图",
        "type": "int",
        "hint": "不发送封面图：0，发送封面图：1",
        "obvious_hint": true,
        "default":0

    },
    "CD_Time":{
        "description": "冷却时间",
        "type": "int",
        "hint": "冷却时间，单位为秒",
        "obvious_hint": true,
        "default":30
    },
    "img_count": {
        "description": "图片数量",
        "type": "int",
        "hint": "发送图片数量，0为不发送",
        "obvious_hint": true,
        "default": 8
    },
     "cover_count": {
        "description": "封面数量",
        "type": "int",
        "hint": "key搜索时候最大发送的封面数量",
        "obvious_hint": true,
        "default": 15
    },
     "schdule_task": {
        "description": "定时任务",
        "type": "int",
        "hint": "是否开启定时任务，0为不开启，其余为开启，默认为0",
        "obvious_hint": true,
        "default": 0
    }
}